version: 1.0
runtime: python3
build:
  commands:
    build:
      - cd src
      - pip install -r requirements.txt
run:
  runtime-version: 3.12
  command: cd src && uvicorn api.app:app --host 0.0.0.0 --port 8080
  network:
    port: 8080 